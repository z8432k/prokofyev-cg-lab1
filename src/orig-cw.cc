#include <iostream>

#include <orig.h>

using namespace std;

int main(int argc, char* argv[] ) {
    cout << "Computer graphic. Control work." << endl << endl;

    orig(argc, argv);

    exit(EXIT_SUCCESS);
}

