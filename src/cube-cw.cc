#include <iostream>

#include <pyramid.h>

using namespace std;

int main(int argc, char* argv[] ) {
    cout << "Computer graphic. Control work." << endl << endl;

    pyramid(argc, argv);

    exit(EXIT_SUCCESS);
}
